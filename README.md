# 2026ReadingWeek

全民阅读活动周 H5 项目。

## 文件

- `index.html`：主页面
- `API.md`：接口说明

## 运行

这是一个静态页面项目，直接部署到 Web 服务器即可。

调试时建议使用本地服务器打开，不要直接用 `file://` 访问。

## 接口

当前前端使用 3 个接口：

- `POST /api/readingweek/check-status`
- `POST /api/readingweek/draw`
- `POST /api/readingweek/submit-address`

